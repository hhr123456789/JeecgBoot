# 产品单耗分析功能 - 测试指南

## 📅 创建时间
2026-02-16

## ✅ 开发完成情况

### 后端部分 (已完成)
- [x] 数据库表设计 (5张表)
  - `tb_product_category` - 产品分类表
  - `tb_product_info` - 产品基础信息表
  - `tb_product_production` - 产品产量记录表
  - `tb_product_process_rel` - 产品工序关联表
  - `tb_product_energy_consumption` - 产品能耗记录表

- [x] 后端代码
  - Entity 实体类 (5个)
  - Mapper 接口和 XML (5个)
  - Service 接口和实现类
  - Controller 控制器 (7个API接口)

### 前端部分 (已完成)
- [x] API 调用服务 (`productEnergy.ts`)
- [x] 主页面 (`index.vue`)
- [x] 图表组件 (4个)
  - ProductPie.vue - 饼图
  - ProductLine.vue - 折线图
  - ProductBar.vue - 柱状图
  - ProductRanking.vue - 排名图

## 🗂️ 文件路径总览

### 数据库脚本
```
jeecg-boot/db/product_energy_001_create_tables.sql    # 建表脚本
jeecg-boot/db/product_energy_002_test_data.sql        # 测试数据
```

### 后端代码
```
jeecg-boot/jeecg-module-energy/src/main/java/org/jeecg/modules/energy/
├── entity/product/
│   ├── TbProductCategory.java
│   ├── TbProductInfo.java
│   ├── TbProductProduction.java
│   ├── TbProductProcessRel.java
│   └── TbProductEnergyConsumption.java
├── mapper/product/
│   ├── TbProductCategoryMapper.java
│   ├── TbProductInfoMapper.java
│   ├── TbProductProductionMapper.java
│   ├── TbProductProcessRelMapper.java
│   └── TbProductEnergyConsumptionMapper.java
├── service/
│   ├── IProductEnergyService.java
│   └── impl/ProductEnergyServiceImpl.java
└── controller/
    └── ProductEnergyController.java
```

### Mapper XML
```
jeecg-boot/jeecg-module-energy/src/main/resources/mapper/product/
├── TbProductCategoryMapper.xml
├── TbProductInfoMapper.xml
├── TbProductProductionMapper.xml
├── TbProductProcessRelMapper.xml
└── TbProductEnergyConsumptionMapper.xml
```

### 前端代码
```
jeecgboot-vue3/src/
├── api/energy/
│   └── productEnergy.ts                  # API 调用服务
└── views/EnergyAnalysis/Energy_Product_Consumption/
    ├── index.vue                         # 主页面
    ├── components/
    │   ├── ProductPie.vue               # 饼图
    │   ├── ProductLine.vue              # 折线图
    │   ├── ProductBar.vue               # 柱状图
    │   └── ProductRanking.vue           # 排名图
    └── README.md                         # 页面说明文档
```

## 🚀 测试步骤

### 步骤1: 执行数据库脚本

#### 1.1 创建数据库表
```bash
# 方式1: 使用 MySQL 命令行
mysql -u root -p EMSProject_jeecg < jeecg-boot/db/product_energy_001_create_tables.sql

# 方式2: 使用 Navicat 或其他数据库工具
# 打开 product_energy_001_create_tables.sql 文件并执行
```

#### 1.2 插入测试数据
```bash
# 方式1: 使用 MySQL 命令行
mysql -u root -p EMSProject_jeecg < jeecg-boot/db/product_energy_002_test_data.sql

# 方式2: 使用 Navicat 或其他数据库工具
# 打开 product_energy_002_test_data.sql 文件并执行
```

#### 1.3 验证数据
```sql
-- 查看数据是否插入成功
USE EMSProject_jeecg;

-- 查看产品分类 (应有10条)
SELECT COUNT(*) FROM tb_product_category;
SELECT * FROM tb_product_category ORDER BY category_level, sort_order;

-- 查看产品信息 (应有10条)
SELECT COUNT(*) FROM tb_product_info;
SELECT * FROM tb_product_info;

-- 查看产量记录 (应有约180条，2024年1-6月的数据)
SELECT COUNT(*) FROM tb_product_production;
SELECT * FROM tb_product_production ORDER BY production_date DESC LIMIT 10;

-- 查看工序关联 (应有40条)
SELECT COUNT(*) FROM tb_product_process_rel;

-- 查看能耗记录 (应有约1080条)
SELECT COUNT(*) FROM tb_product_energy_consumption;
SELECT * FROM tb_product_energy_consumption ORDER BY record_time DESC LIMIT 10;
```

### 步骤2: 启动后端服务

#### 2.1 使用开发模式启动
```bash
cd jeecg-boot
npm run dev
# 或者在 Windows 上运行
start-dev.bat
```

#### 2.2 或使用 Maven 启动
```bash
cd jeecg-boot/jeecg-module-system/jeecg-system-start
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

#### 2.3 验证后端服务
```bash
# 后端服务默认端口: 8080
# 访问 Swagger 文档查看接口
http://localhost:8080/jeecg-boot/swagger-ui/index.html
# 搜索 "产品能耗" 或 "ProductEnergy"
```

### 步骤3: 测试后端API接口

#### 3.1 使用 Postman 或 curl 测试

**测试1: 获取统计数据**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/statistics?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30"
```

**测试2: 获取能耗分布**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/distribution?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30"
```

**测试3: 获取单耗趋势**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/trend?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30"
```

**测试4: 获取产量能耗对比**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/comparison?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30"
```

**测试5: 获取单耗排名**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/ranking?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30&order=asc"
```

**测试6: 获取明细列表**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/detail-list?timeDimension=month&startDate=2024-01-01&endDate=2024-06-30&pageNo=1&pageSize=10"
```

**测试7: 获取产品分类树**
```bash
curl -X GET "http://localhost:8080/jeecg-boot/energy/product/category-tree"
```

### 步骤4: 启动前端服务

```bash
cd jeecgboot-vue3
pnpm install   # 如果尚未安装依赖
pnpm dev
```

前端服务默认端口: 3100
访问地址: http://localhost:3100

### 步骤5: 前端功能测试

#### 5.1 登录系统
- 用户名: admin
- 密码: (您的系统密码)

#### 5.2 访问页面
导航到: **能源分析 > 产品单耗分析**
或直接访问: http://localhost:3100/#/EnergyAnalysis/Energy_Product_Consumption

#### 5.3 功能测试清单

**测试项1: 左侧产品分类树**
- [ ] 树形菜单是否正确显示产品分类
- [ ] 展开/折叠功能是否正常
- [ ] 搜索功能是否可用
- [ ] 点击节点后数据是否更新

**测试项2: 顶部筛选区**
- [ ] 时间维度切换 (日/月/年) 是否正常
- [ ] 日期选择器是否正确显示对应类型 (日期/月份/年份)
- [ ] 查询按钮是否能触发数据刷新

**测试项3: 数据卡片区**
- [ ] 总能耗数据是否正确显示
- [ ] 总产量数据是否正确显示
- [ ] 合格产量数据是否正确显示
- [ ] 合格率数据是否正确显示 (应为百分比)
- [ ] 单位产品能耗是否正确显示

**测试项4: 图表区域**
- [ ] 产品能耗分布饼图是否正确渲染
- [ ] 产品单耗趋势折线图是否正确显示
- [ ] 产量能耗对比柱状图是否显示双轴
- [ ] 产品单耗排名横向柱状图是否正确渲染
- [ ] 鼠标悬停提示是否正常工作
- [ ] 图表响应式缩放是否正常

**测试项5: 数据表格**
- [ ] 表格数据是否正确显示
- [ ] 分页功能是否正常
- [ ] 环比列是否显示颜色 (红升绿降)
- [ ] 导出按钮是否可点击 (目前显示提示信息)

**测试项6: 数据准确性**
- [ ] 统计数据与数据库查询结果是否一致
- [ ] 图表数据与数据库数据是否匹配
- [ ] 单位产品能耗计算是否正确 (总能耗 ÷ 合格产量)
- [ ] 合格率计算是否正确 (合格产量 ÷ 总产量 × 100%)

### 步骤6: 异常情况测试

**测试1: 无数据情况**
- 选择一个没有数据的时间范围
- 检查页面是否有友好提示

**测试2: 网络错误**
- 停止后端服务
- 检查前端是否有错误提示

**测试3: 参数边界**
- 测试极端日期范围
- 测试空参数情况

## 📊 预期测试数据

### 2024年1-6月统计数据 (全部产品)

根据测试数据，预期结果应该类似:

| 指标 | 预期值 (约) |
|------|----------|
| 总能耗 | 约 3,500,000 kWh |
| 总产量 | 约 12,000 件 |
| 合格产量 | 约 11,500 件 |
| 合格率 | 约 95-98% |
| 单位产品能耗 | 约 300-700 kWh/件 (因产品而异) |

### 产品分类树结构

```
铝型材产品
├── 建筑型材
│   ├── 门窗型材 (2个产品)
│   └── 幕墙型材 (2个产品)
└── 工业型材
    ├── 散热器型材 (2个产品)
    └── 汽车型材 (2个产品)

铝板带产品
├── 中厚板 (1个产品)
└── 薄板 (1个产品)
```

## 🐛 常见问题排查

### 问题1: 后端启动失败
**可能原因**:
- 数据库连接配置错误
- 端口 8080 被占用
- Maven 依赖未正确安装

**解决方案**:
```bash
# 检查配置文件
vim jeecg-boot/jeecg-module-system/jeecg-system-start/src/main/resources/application-dev.yml

# 检查端口占用
netstat -ano | findstr :8080

# 重新编译
cd jeecg-boot
mvn clean install -DskipTests
```

### 问题2: 前端页面空白
**可能原因**:
- 后端服务未启动
- API 接口路径错误
- 跨域问题

**解决方案**:
- 检查浏览器控制台错误信息
- 验证后端服务是否运行
- 检查 API 路径配置

### 问题3: 数据不显示
**可能原因**:
- 数据库表为空
- SQL 脚本未执行
- 日期范围选择错误

**解决方案**:
```sql
-- 检查数据
SELECT COUNT(*) FROM tb_product_production WHERE production_date BETWEEN '2024-01-01' AND '2024-06-30';
SELECT COUNT(*) FROM tb_product_energy_consumption WHERE record_time BETWEEN '2024-01-01' AND '2024-06-30';
```

### 问题4: 图表不显示
**可能原因**:
- ECharts 未正确安装
- 数据格式不匹配
- DOM 元素未正确渲染

**解决方案**:
- 检查浏览器控制台错误
- 查看 Network 标签中的 API 响应
- 确认 ECharts 依赖已安装

## ✅ 测试验收标准

### 功能完整性
- [ ] 所有7个API接口都能正常返回数据
- [ ] 前端页面所有功能模块都能正常显示
- [ ] 左侧树、筛选、卡片、图表、表格都能正常工作

### 数据准确性
- [ ] 统计数据计算正确
- [ ] 图表数据与数据库一致
- [ ] 单位产品能耗公式计算正确

### 用户体验
- [ ] 页面加载流畅，无明显卡顿
- [ ] 图表交互响应及时
- [ ] 错误提示友好
- [ ] 样式美观，布局合理

### 性能要求
- [ ] 页面首次加载时间 < 3秒
- [ ] 数据查询响应时间 < 1秒
- [ ] 图表渲染时间 < 500ms

## 📝 测试记录模板

```
测试日期: ____________________
测试人员: ____________________

【环境信息】
- 操作系统: ____________________
- 浏览器: ____________________
- 后端版本: ____________________
- 前端版本: ____________________

【测试结果】
1. 数据库脚本执行: ☐ 通过 ☐ 失败
2. 后端服务启动: ☐ 通过 ☐ 失败
3. API接口测试: ☐ 通过 ☐ 失败
4. 前端服务启动: ☐ 通过 ☐ 失败
5. 页面功能测试: ☐ 通过 ☐ 失败
6. 数据准确性验证: ☐ 通过 ☐ 失败

【发现问题】
1. ____________________
2. ____________________
3. ____________________

【备注】
____________________
____________________
```

## 🎯 下一步优化建议

### 功能优化
1. 实现真实的导出功能 (Excel/PDF)
2. 添加数据缓存机制提升性能
3. 实现产品搜索和高级筛选
4. 添加数据对比功能 (同比、环比)
5. 实现自定义时间范围选择

### 性能优化
1. 实现前端数据缓存
2. 优化数据库查询 (添加索引)
3. 实现分页加载大数据集
4. 图表懒加载和虚拟滚动

### 用户体验优化
1. 添加骨架屏加载效果
2. 优化移动端适配
3. 添加数据钻取功能
4. 实现数据告警和推送

---

**文档版本**: v1.0
**最后更新**: 2026-02-16
**维护人**: AI Assistant
