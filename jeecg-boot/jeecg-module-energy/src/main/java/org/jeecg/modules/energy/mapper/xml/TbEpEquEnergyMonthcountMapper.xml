<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.energy.mapper.TbEpEquEnergyMonthcountMapper">

    <select id="selectByModuleIdAndMonth" resultType="org.jeecg.modules.energy.entity.TbEpEquEnergyMonthcount">
        SELECT * FROM tb_ep_equ_energy_monthcount 
        WHERE module_id = #{moduleId} 
        AND YEAR(dt) = YEAR(#{date}) 
        AND MONTH(dt) = MONTH(#{date})
    </select>

</mapper>