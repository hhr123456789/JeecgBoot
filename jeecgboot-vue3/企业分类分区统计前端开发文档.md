# 企业分类分区统计功能前端开发文档

## 一、功能概述

企业分类分区统计功能前端主要实现以下功能：

1. 左侧部门树形结构展示和选择
2. 顶部查询条件区域（时间选择、能源类型选择）
3. 统计卡片展示（总能耗、分能源消耗）
4. 图表展示区域（饼图占比、折线图趋势）
5. 详细数据表格展示
6. 数据导出功能

## 二、页面结构

### 2.1 页面布局
```
┌─────────────────────────────────────────────────────────────┐
│                    企业分类分区统计                           │
├─────────────┬───────────────────────────────────────────────┤
│             │                查询条件区域                      │
│   部门树     ├───────────────────────────────────────────────┤
│   形结构     │                统计卡片区域                      │
│             ├───────────────────────────────────────────────┤
│             │         图表区域（饼图+折线图）                  │
│             ├───────────────────────────────────────────────┤
│             │                数据表格区域                      │
└─────────────┴───────────────────────────────────────────────┘
```

### 2.2 组件结构
```
Energy_Classification/
├── index.vue                           # 主页面组件
├── components/
│   ├── EnergyDistributionPie.vue      # 能源分布饼图组件
│   ├── EnergyTrendLine.vue            # 能源趋势折线图组件
│   └── SearchForm.vue                 # 查询表单组件
├── hooks/
│   └── useEnergyClassification.ts     # 业务逻辑Hook
├── types/
│   └── index.ts                       # TypeScript类型定义
└── api/
    └── index.ts                       # API接口定义
```

## 三、主要功能实现

### 3.1 部门树形结构

#### 3.1.1 数据结构
```typescript
interface OrgTreeNode {
  id: string;
  orgCode: string;
  orgName: string;
  parentId: string;
  children?: OrgTreeNode[];
}
```

#### 3.1.2 功能实现
```vue
<template>
  <a-tree
    v-model:selectedKeys="selectedKeys"
    v-model:expandedKeys="expandedKeys"
    :tree-data="treeData"
    :field-names="{ title: 'orgName', key: 'id', children: 'children' }"
    @select="handleOrgSelect"
  />
</template>

<script setup lang="ts">
const selectedKeys = ref<string[]>([]);
const expandedKeys = ref<string[]>(['1']);

const handleOrgSelect = (selectedKeys: string[], info: any) => {
  if (selectedKeys.length > 0) {
    queryParam.orgCode = info.node.orgCode;
    loadData();
  }
};
</script>
```

### 3.2 查询条件区域

#### 3.2.1 时间维度选择
```vue
<template>
  <a-radio-group v-model:value="timeUnit" @change="handleTimeUnitChange">
    <a-radio-button value="day">日</a-radio-button>
    <a-radio-button value="month">月</a-radio-button>
    <a-radio-button value="year">年</a-radio-button>
  </a-radio-group>
  
  <a-date-picker 
    v-model:value="selectedDate"
    :format="dateFormat"
    :picker="pickerType"
  />
</template>

<script setup lang="ts">
const timeUnit = ref<'day' | 'month' | 'year'>('month');
const selectedDate = ref<Dayjs>(dayjs());

const dateFormat = computed(() => {
  switch (timeUnit.value) {
    case 'day': return 'YYYY-MM-DD';
    case 'month': return 'YYYY-MM';
    case 'year': return 'YYYY';
    default: return 'YYYY-MM-DD';
  }
});

const pickerType = computed(() => {
  switch (timeUnit.value) {
    case 'day': return 'date';
    case 'month': return 'month';
    case 'year': return 'year';
    default: return 'date';
  }
});
</script>
```

#### 3.2.2 能源类型选择
```vue
<template>
  <a-select v-model:value="energyType" @change="handleEnergyTypeChange">
    <a-select-option value="all">全部能源</a-select-option>
    <a-select-option value="1">电能</a-select-option>
    <a-select-option value="2">水能</a-select-option>
    <a-select-option value="3">燃气</a-select-option>
  </a-select>
</template>

<script setup lang="ts">
const energyType = ref<string>('all');

const handleEnergyTypeChange = () => {
  queryParam.energyType = energyType.value;
  loadData();
};
</script>
```

### 3.3 统计卡片展示

#### 3.3.1 响应式数据绑定
```vue
<template>
  <div class="grid grid-cols-4 gap-4">
    <div class="bg-white rounded-lg p-4">
      <div class="text-gray-600 mb-2">总能耗</div>
      <div class="text-2xl font-bold">{{ formatNumber(statisticsData.totalConsumption) }}</div>
      <div class="text-sm text-gray-500 mt-1">单位: {{ getUnit() }}</div>
    </div>
    <!-- 其他卡片类似 -->
  </div>
</template>

<script setup lang="ts">
const statisticsData = reactive({
  totalConsumption: 0,
  electricConsumption: 0,
  waterConsumption: 0,
  gasConsumption: 0,
  totalCost: 0,
  totalCarbonEmission: 0
});

const formatNumber = (num: number) => {
  return num.toLocaleString('zh-CN', {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
  });
};

const getUnit = () => {
  if (energyType.value === 'all') return '综合单位';
  const type = energyTypes.find(t => t.energyType === parseInt(energyType.value));
  return type?.energyUnit || '';
};
</script>
```

### 3.4 图表展示

#### 3.4.1 能源分布饼图
```vue
<!-- EnergyDistributionPie.vue -->
<template>
  <div ref="chartRef" style="width: 100%; height: 300px;"></div>
</template>

<script setup lang="ts">
import * as echarts from 'echarts';
import { onMounted, watch, ref } from 'vue';

const props = defineProps<{
  chartData: PieChartData;
}>();

const chartRef = ref<HTMLElement>();
let chartInstance: echarts.ECharts | null = null;

const initChart = () => {
  if (!chartRef.value) return;
  
  chartInstance = echarts.init(chartRef.value);
  const option = {
    tooltip: {
      trigger: 'item',
      formatter: '{a} <br/>{b}: {c} ({d}%)'
    },
    legend: {
      orient: 'vertical',
      left: 'left'
    },
    series: props.chartData.series
  };
  
  chartInstance.setOption(option);
};

onMounted(() => {
  initChart();
});

watch(() => props.chartData, () => {
  if (chartInstance) {
    initChart();
  }
}, { deep: true });
</script>
```

#### 3.4.2 能源趋势折线图
```vue
<!-- EnergyTrendLine.vue -->
<template>
  <div ref="chartRef" style="width: 100%; height: 300px;"></div>
</template>

<script setup lang="ts">
import * as echarts from 'echarts';
import { onMounted, watch, ref } from 'vue';

const props = defineProps<{
  chartData: LineChartData;
}>();

const chartRef = ref<HTMLElement>();
let chartInstance: echarts.ECharts | null = null;

const initChart = () => {
  if (!chartRef.value) return;
  
  chartInstance = echarts.init(chartRef.value);
  const option = {
    tooltip: {
      trigger: 'axis'
    },
    legend: {
      data: props.chartData.series.map(s => s.name)
    },
    xAxis: props.chartData.xAxis,
    yAxis: {
      type: 'value',
      name: '消耗量'
    },
    series: props.chartData.series
  };
  
  chartInstance.setOption(option);
};

onMounted(() => {
  initChart();
});
</script>
```

### 3.5 数据表格

#### 3.5.1 动态列配置
```vue
<template>
  <a-table
    :columns="dynamicColumns"
    :data-source="tableData"
    :pagination="paginationConfig"
    :loading="loading"
  />
</template>

<script setup lang="ts">
const dynamicColumns = computed(() => {
  const baseColumns = [
    { title: '时间', dataIndex: 'time', width: 120 }
  ];
  
  if (energyType.value === 'all') {
    return [
      ...baseColumns,
      { title: '电能消耗(kWh)', dataIndex: 'electric', align: 'right' },
      { title: '水能消耗(m³)', dataIndex: 'water', align: 'right' },
      { title: '燃气消耗(m³)', dataIndex: 'gas', align: 'right' },
      { title: '总成本(元)', dataIndex: 'totalCost', align: 'right' }
    ];
  } else {
    const type = energyTypes.find(t => t.energyType === parseInt(energyType.value));
    return [
      ...baseColumns,
      { 
        title: `${type?.energyName}消耗(${type?.energyUnit})`, 
        dataIndex: 'consumption', 
        align: 'right' 
      },
      { title: '成本(元)', dataIndex: 'cost', align: 'right' },
      { title: '碳排放(kg)', dataIndex: 'carbonEmission', align: 'right' }
    ];
  }
});
</script>
```

### 3.6 数据导出功能

#### 3.6.1 导出实现
```typescript
const handleExport = async () => {
  try {
    const params = {
      orgCode: queryParam.orgCode,
      energyType: queryParam.energyType,
      timeDimension: queryParam.timeDimension,
      startDate: queryParam.startDate,
      endDate: queryParam.endDate,
      includeChildren: queryParam.includeChildren
    };
    
    const response = await exportClassificationData(params);
    const blob = new Blob([response], { type: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet' });
    const url = window.URL.createObjectURL(blob);
    const link = document.createElement('a');
    link.href = url;
    link.download = `能源分类统计_${dayjs().format('YYYYMMDD_HHmmss')}.xlsx`;
    link.click();
    window.URL.revokeObjectURL(url);
    
    message.success('导出成功');
  } catch (error) {
    message.error('导出失败');
  }
};
```

## 四、状态管理

### 4.1 使用Pinia进行状态管理
```typescript
// stores/energyClassification.ts
import { defineStore } from 'pinia';
import { ref } from 'vue';

export const useEnergyClassificationStore = defineStore('energyClassification', () => {
  const selectedOrg = ref<string>('');
  const timeUnit = ref<'day' | 'month' | 'year'>('month');
  const energyType = ref<string>('all');
  const dateRange = ref<string[]>([]);
  
  return {
    selectedOrg,
    timeUnit,
    energyType,
    dateRange
  };
});
```

## 五、API接口定义

### 5.1 接口封装
```typescript
// api/energyClassification.ts
import { defHttp } from '/@/utils/http/axios';
import { ClassificationQueryParam, ClassificationSummaryVO, TrendDataVO, OrgTreeVO, EnergyTypeVO } from './model';

enum Api {
  getOrgTree = '/energy/classification/getOrgTree',
  getEnergyTypes = '/energy/classification/getEnergyTypes',
  getSummaryData = '/energy/classification/getSummaryData',
  getTrendData = '/energy/classification/getTrendData',
  exportData = '/energy/classification/exportData'
}

// 获取部门树
export const getOrgTree = () => {
  return defHttp.get<OrgTreeVO[]>({ url: Api.getOrgTree });
};

// 获取能源类型
export const getEnergyTypes = () => {
  return defHttp.get<EnergyTypeVO[]>({ url: Api.getEnergyTypes });
};

// 获取汇总数据
export const getSummaryData = (params: ClassificationQueryParam) => {
  return defHttp.post<ClassificationSummaryVO>({ url: Api.getSummaryData, params });
};

// 获取趋势数据
export const getTrendData = (params: ClassificationQueryParam) => {
  return defHttp.post<TrendDataVO>({ url: Api.getTrendData, params });
};

// 导出数据
export const exportClassificationData = (params: ClassificationQueryParam) => {
  return defHttp.post({ 
    url: Api.exportData, 
    params,
    responseType: 'blob'
  }, { isTransformResponse: false });
};
```

## 六、TypeScript类型定义

### 6.1 主要类型定义
```typescript
// types/index.ts
export interface OrgTreeVO {
  id: string;
  orgCode: string;
  orgName: string;
  parentId: string;
  children?: OrgTreeVO[];
}

export interface EnergyTypeVO {
  energyType: number;
  energyName: string;
  energyUnit: string;
  pricePerUnit: number;
}

export interface ClassificationQueryParam {
  orgCode: string;
  energyType: string;
  timeDimension: 'day' | 'month' | 'year';
  startDate: string;
  endDate: string;
  includeChildren: boolean;
}

export interface ClassificationSummaryVO {
  statisticsData: StatisticsDataVO;
  pieChartData: PieChartDataVO;
  tableData: TableDataVO[];
}

export interface StatisticsDataVO {
  totalConsumption: number;
  electricConsumption: number;
  waterConsumption: number;
  gasConsumption: number;
  totalCost: number;
  totalCarbonEmission: number;
}

export interface PieChartDataVO {
  series: Array<{
    name: string;
    type: string;
    data: Array<{
      value: number;
      name: string;
      percentage: number;
    }>;
  }>;
}

export interface LineChartDataVO {
  xAxis: {
    type: string;
    data: string[];
  };
  series: Array<{
    name: string;
    type: string;
    data: number[];
  }>;
}
```

## 七、样式设计

### 7.1 使用Tailwind CSS
```vue
<template>
  <div class="energy-classification-container flex min-h-screen bg-gray-50 p-4">
    <!-- 左侧部门树 -->
    <div class="w-64 bg-white p-4 mr-4 rounded-lg shadow-sm">
      <div class="flex items-center justify-between mb-4">
        <span class="text-base font-medium text-gray-700">部门列表</span>
        <a-input-search
          v-model:value="searchText"
          placeholder="搜索部门"
          class="w-32"
          size="small"
        />
      </div>
      <!-- 树形组件 -->
    </div>
    
    <!-- 右侧内容区 -->
    <div class="flex-1">
      <!-- 查询条件 -->
      <div class="mb-4 bg-white rounded-lg p-4 shadow-sm">
        <div class="flex items-center gap-4">
          <!-- 查询控件 -->
        </div>
      </div>
      
      <!-- 统计卡片 -->
      <div class="grid grid-cols-4 gap-4 mb-4">
        <div class="bg-white rounded-lg p-4 shadow-sm border border-gray-100">
          <div class="text-gray-600 mb-2 text-sm">总能耗</div>
          <div class="text-2xl font-bold text-gray-800">{{ formatNumber(statisticsData.totalConsumption) }}</div>
          <div class="text-sm text-gray-500 mt-1">单位: {{ getUnit() }}</div>
        </div>
      </div>
    </div>
  </div>
</template>
```

## 八、性能优化

### 8.1 图表性能优化
1. 使用图表懒加载
2. 防抖处理频繁的数据更新
3. 图表销毁时释放内存

### 8.2 数据加载优化
1. 分页加载表格数据
2. 使用虚拟滚动处理大量数据
3. 缓存部门树等静态数据

### 8.3 响应式设计
1. 适配不同屏幕尺寸
2. 移动端优化显示
3. 自适应布局调整

## 九、错误处理

### 9.1 统一错误处理
```typescript
const loadData = async () => {
  try {
    loading.value = true;
    const response = await getSummaryData(queryParam);
    updateData(response);
  } catch (error) {
    console.error('加载数据失败:', error);
    message.error('加载数据失败，请稍后重试');
  } finally {
    loading.value = false;
  }
};
```

## 十、测试要点

1. 测试部门树的选择和联动
2. 测试不同时间维度的切换
3. 测试能源类型筛选功能
4. 测试图表数据的正确性
5. 测试表格排序和分页
6. 测试数据导出功能
7. 测试响应式布局
8. 测试错误处理和加载状态